# data/

One folder per continuous recording: `YYYYMMDD_run##_<config>/` containing
`puck.bin` (raw WitMotion SD file, renamed), `racebox.csv`, `notes.md`
(config, PASM mode, cold/hot pressures, fuel ¼–¾, ambient, surface).
Raw folders go public with the write-up posts.
